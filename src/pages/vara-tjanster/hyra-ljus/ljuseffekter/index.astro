---
import Layout from '../../../../layouts/Layout.astro';
import CategoryHero from '../../../../components/CategoryHero.astro';
import MediaCard from '../../../../components/MediaCard.astro';
import CartButton from '../../../../components/CartButton.astro';
import ElTillbehorCard from '../../../../components/ElTillbehorCard.astro';
import ljusData from '../../../../data/ljus.json';
import _elData from '../../../../data/el.json';

const WP = '/videos';

// Alla produkter läses från JSON — inga hårdkodade produktlistor här
type Effekt = typeof ljusData.effekter.products[0] & { group?: string; tag?: string; video?: string; volumePricing?: {minQty:number,unitPrice:number}[] };
const allEffekter = ljusData.effekter.products as unknown as Effekt[];

// Volymrader från JSON — inga hårdkodade priser. data-exkl ger moms-toggle-stöd via global applyVat()
function volRows(artno: string) {
  const p = allEffekter.find(x => x.artno === artno);
  const tiers = p?.volumePricing;
  if (!tiers?.length) return [];
  return [{ label: 'Volympriser', rows: tiers.map(t => {
    const total = t.unitPrice * t.minQty;
    return `${t.minQty} st – <span data-exkl="${total}">${total.toLocaleString('sv')}</span> kr`;
  }) }];
}

// Hjälpfunktion för att bygga MediaCard-props från JSON-produkt
function toCard(p: Effekt) {
  return {
    id: p.slug,        // cart-ID = slug — säkerställer match mot PROD_CATALOG
    title: p.name,
    artno: p.artno,
    img: p.image,
    excl: p.price,
    videos: p.video ? [`${WP}/${p.video}`] : [],
    rows: volRows(p.artno),
    bullets: p.includes ?? [],
    tag: p.tag,
    manualUrl: (p as any).manualUrl,
  };
}

// Gruppering styrs av group-fältet i JSON
const ledPar      = allEffekter.filter(p => p.group === 'led-par').map(toCard);
const specialEffekter = allEffekter.filter(p => p.group === 'special').map(toCard);
const movingHeads = allEffekter.filter(p => p.group === 'moving-head').map(toCard);
const faqSchema = {
  "@context": "https://schema.org",
  "@type": "FAQPage",
  "mainEntity": ljusData.effekter.faq.map((f: {q:string,a:string}) => ({
    "@type": "Question",
    "name": f.q,
    "acceptedAnswer": { "@type": "Answer", "text": f.a }
  }))
};

---
<Layout
  schemaExtra={faqSchema}
  title="Hyr Lösa Ljuseffekter Stockholm – LED PAR, Moving Heads & Laser | Scenkonsult"
  description="Hyr lösa ljuseffekter i Stockholm från 79 kr/dygn. LED PAR, Moving Head, Scanner, Strobe, Laser, LED Bar. Volympriser. Leverans i hela Storstockholm."
>

  <CategoryHero
    h1="Ljuseffekter"
    h1Accent="Stockholm"
    intro="Hyr lösa ljuseffekter i Stockholm. LED PAR, Moving Heads, stroboskop, UV, laser och scanners – bygg din egna ljussetup. Volympriser för fler enheter. Leverans i hela Storstockholm."
    breadcrumbs={[
      { label: "Hem", href: "/" },
      { label: "Hyra ljus", href: "/vara-tjanster/hyra-ljus/" },
      { label: "Ljuseffekter" }
    ]}
    badges={[
      "LED PAR & moving heads",
      "Stroboskop & UV & laser",
      "DMX-styrda",
      "Leverans i Storstockholm",
    ]}
    priceFrom={79}
  />

  <!-- LED PAR -->
  <section class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pb-12">
    <h2 class="text-2xl font-bold text-white mb-6">LED PAR & armaturer</h2>
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
      {ledPar.map(p => <MediaCard title={p.title} images={[p.img]} videos={p.videos ?? []} priceExcl={p.excl} rows={p.rows} bullets={p.bullets} artno={p.artno} category="Ljuseffekter" manualUrl={p.manualUrl} />)}
    </div>
  </section>

  <!-- Special effekter -->
  <section class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pb-12">
    <h2 class="text-2xl font-bold text-white mb-6">Scanner, Strobe & Laser</h2>
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
      {specialEffekter.map(p => <MediaCard title={p.title} images={[p.img]} videos={p.videos ?? []} priceExcl={p.excl} rows={p.rows} bullets={p.bullets} artno={p.artno} category="Ljuseffekter" manualUrl={p.manualUrl} />)}
    </div>
  </section>

  <!-- Moving Heads -->
  <section class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pb-16">
    <div class="flex flex-col sm:flex-row sm:items-end gap-3 mb-6">
      <div>
        <h2 class="text-2xl font-bold text-white">Moving Heads</h2>
        <p class="text-gray-400 text-sm mt-1">Stockholms största utbud — Wash, Beam/Spot och LED Bar.</p>
      </div>
      <a href="/vara-tjanster/hyra-ljus/moving-heads/" class="sm:ml-auto text-brand-orange hover:text-brand-orange-light text-sm font-semibold shrink-0 transition-colors">
        Se alla Moving Heads & guide →
      </a>
    </div>
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
      {movingHeads.map(p => <MediaCard title={p.title} tag={p.tag} images={[p.img]} videos={p.videos ?? []} priceExcl={p.excl} rows={p.rows} bullets={p.bullets} artno={p.artno} category="Ljuseffekter" manualUrl={p.manualUrl} />)}
    </div>
  </section>

  <!-- Komplettera med stativ -->
  <section class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pb-12">
    <div class="bg-brand-navy border border-white/10 rounded-2xl p-6">
      <h2 class="text-xl font-bold text-white mb-2">Komplettera med stativ & tross</h2>
      <p class="text-gray-400 text-sm mb-4">Alla lösa armaturer behöver monteras på stativ eller tross. Se vårt sortiment av stativ, trosstorn och tillbehör.</p>
      <a href="/vara-tjanster/hyra-ljus/stativ-tross/" class="inline-block bg-brand-orange hover:bg-brand-orange-light text-white px-5 py-2.5 rounded-xl text-sm font-semibold transition-colors">Se stativ & tross →</a>
    </div>
  </section>


  <!-- LJUSSTYRNING (DMX) -->
  <section class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pb-16">
    <h2 class="text-2xl font-bold text-white mb-6 border-b border-white/10 pb-3">Ljusstyrning (DMX)</h2>
    <div class="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-5 gap-3">
      {(ljusData.dmx?.tillbehor ?? []).map((p: any) => (
        <ElTillbehorCard img={p.image} name={p.name} excl={p.price} desc={p.desc ?? ''} category="DMX-styrning" artno={p.artno} modal={(p.price ?? 0) >= 400} specs={p.specs ?? []} manualUrl={p.manualUrl} />
      ))}
    </div>
  </section>

  <!-- EL-TILLBEHÖR -->
  <section class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pb-16">
    <h2 class="text-2xl font-bold text-white mb-6 border-b border-white/10 pb-3">El-tillbehör</h2>
    <div class="grid grid-cols-3 sm:grid-cols-4 lg:grid-cols-7 gap-3">
      {[
        ...(_elData.products?.filter((p:any) => ['SK-EL-0001','SK-EL-0003','SK-EL-0002'].includes(p.artno))
          .sort((a:any,b:any) => ['SK-EL-0001','SK-EL-0003','SK-EL-0002'].indexOf(a.artno) - ['SK-EL-0001','SK-EL-0003','SK-EL-0002'].indexOf(b.artno)) ?? []),
        ...(_elData.products?.filter((p:any) => !['SK-EL-0001','SK-EL-0002','SK-EL-0003'].includes(p.artno)) ?? []),
      ].map((p: any) => (
        <ElTillbehorCard img={p.image} name={p.name} excl={p.price} desc={p.desc ?? ''} category="El-tillbehör" artno={p.artno} />
      ))}
      {(ljusData.rok?.tillbehor?.filter((p:any) => ['SK-LJS-ROK-ACC-0001','SK-LJS-ROK-ACC-0002'].includes(p.artno)) ?? []).map((p: any) => (
        <ElTillbehorCard img={p.image} name={p.name} excl={p.price} desc={p.desc ?? ''} category="El-tillbehör" artno={p.artno} priceNote={p.priceNote ?? '/st'} />
      ))}
    </div>
  </section>

  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pb-16 flex flex-wrap gap-4">
    <a href="/vara-tjanster/hyra-ljus/" class="border border-white/20 text-gray-300 hover:border-brand-orange hover:text-brand-orange px-5 py-3 rounded-xl text-sm font-semibold transition-all">← Tillbaka till ljus</a>
    <a href="/vara-tjanster/hyra-ljus/fardiga-paket/" class="border border-white/30 text-white hover:bg-white/10 px-5 py-3 rounded-xl text-sm font-semibold transition-colors">Färdiga paket →</a>
    <a href="/bokningssida/" class="bg-brand-orange hover:bg-brand-orange-light text-white px-5 py-3 rounded-xl text-sm font-semibold transition-colors">Boka nu</a>
  </div>

  <!-- Guider & Svens Kunskapsskola -->
  <section class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pb-12">
    <div style="background:#1e1850;border:1px solid rgba(255,255,255,0.08);border-radius:16px;padding:1.75rem;">
      <p style="color:#c4b5f4;font-family:'Space Grotesk',sans-serif;font-size:0.72rem;font-weight:700;text-transform:uppercase;letter-spacing:0.1em;margin:0 0 1rem;">Läs mer & förbered dig</p>
      <div style="display:flex;flex-wrap:wrap;gap:2.5rem;">
        <div style="flex:1;min-width:220px;">
          <p style="color:#c4b5f4;font-family:'Space Grotesk',sans-serif;font-size:0.72rem;font-weight:700;text-transform:uppercase;letter-spacing:0.1em;margin:0 0 0.5rem;">Guider</p>
        <a href="/for/guider/ljussattning-tips/" style="display:flex;align-items:center;gap:0.5rem;color:rgba(255,255,255,0.7);font-size:0.88rem;text-decoration:none;padding:0.5rem 0;border-bottom:1px solid rgba(255,255,255,0.05);transition:color 0.15s;" onmouseenter="this.style.color='#c4b5f4'" onmouseleave="this.style.color='rgba(255,255,255,0.7)'"><svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" style="flex-shrink:0;opacity:0.5;"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/></svg>Ljussättningstips för event</a>
        <a href="/for/guider/hyra-uplights/" style="display:flex;align-items:center;gap:0.5rem;color:rgba(255,255,255,0.7);font-size:0.88rem;text-decoration:none;padding:0.5rem 0;border-bottom:1px solid rgba(255,255,255,0.05);transition:color 0.15s;" onmouseenter="this.style.color='#c4b5f4'" onmouseleave="this.style.color='rgba(255,255,255,0.7)'"><svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" style="flex-shrink:0;opacity:0.5;"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/></svg>Guide: Hyra uplights</a>
        <a href="/for/guider/hyra-rokmaskin/" style="display:flex;align-items:center;gap:0.5rem;color:rgba(255,255,255,0.7);font-size:0.88rem;text-decoration:none;padding:0.5rem 0;border-bottom:1px solid rgba(255,255,255,0.05);transition:color 0.15s;" onmouseenter="this.style.color='#c4b5f4'" onmouseleave="this.style.color='rgba(255,255,255,0.7)'"><svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" style="flex-shrink:0;opacity:0.5;"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/></svg>Guide: Hyra rökmaskin</a>
          <a href="/for/guider/" style="display:inline-block;margin-top:0.6rem;color:rgba(255,255,255,0.60);font-size:0.8rem;text-decoration:none;">Alla guider →</a>
        </div>
        <div style="flex:1;min-width:220px;">
          <p style="color:#c4b5f4;font-family:'Space Grotesk',sans-serif;font-size:0.72rem;font-weight:700;text-transform:uppercase;letter-spacing:0.1em;margin:0 0 0.5rem;">Svens Kunskapsskola</p>
        <a href="/svens-kunskapsskola/#led-teknik" style="display:flex;align-items:center;gap:0.5rem;color:rgba(255,255,255,0.7);font-size:0.88rem;text-decoration:none;padding:0.5rem 0;border-bottom:1px solid rgba(255,255,255,0.05);transition:color 0.15s;" onmouseenter="this.style.color='#c4b5f4'" onmouseleave="this.style.color='rgba(255,255,255,0.7)'"><svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="#c4b5f4" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" style="flex-shrink:0;"><polygon points="23 7 16 12 23 17 23 7"/><rect x="1" y="5" width="15" height="14" rx="2" ry="2"/></svg>Video: LED-teknik — egenskaper och funktioner</a>
        <a href="/svens-kunskapsskola/#kontakter-bild" style="display:flex;align-items:center;gap:0.5rem;color:rgba(255,255,255,0.7);font-size:0.88rem;text-decoration:none;padding:0.5rem 0;border-bottom:1px solid rgba(255,255,255,0.05);transition:color 0.15s;" onmouseenter="this.style.color='#c4b5f4'" onmouseleave="this.style.color='rgba(255,255,255,0.7)'"><svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="#c4b5f4" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" style="flex-shrink:0;"><polygon points="23 7 16 12 23 17 23 7"/><rect x="1" y="5" width="15" height="14" rx="2" ry="2"/></svg>Video: Kontakter inom bild</a>
          <a href="/svens-kunskapsskola/" style="display:inline-block;margin-top:0.6rem;color:#c4b5f4;font-size:0.8rem;font-weight:600;text-decoration:none;">Alla videoguider →</a>
        </div>
      </div>
    </div>
  </section>

</Layout>
