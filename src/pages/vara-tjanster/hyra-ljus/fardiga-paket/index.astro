---
import Layout from '../../../../layouts/Layout.astro';
import KunskapBlock from '../../../../components/KunskapBlock.astro';
import ModernHero from '../../../../components/ModernHero.astro';
import MediaCard from '../../../../components/MediaCard.astro';
import CartButton from '../../../../components/CartButton.astro';
import ElTillbehorCard from '../../../../components/ElTillbehorCard.astro';
import ljusData from '../../../../data/ljus.json';
import _elData from '../../../../data/el.json';
import _tjansterData from '../../../../data/tjanster.json';

// JSON-first: alla paket kommer från ljus.json paket.products
const WP = '/videos';

type Paket = typeof ljusData.paket.products[0] & {
  videos?: string[];
  manualUrl?: string;
  group?: string;
};

// Mappa JSON-produkt → MediaCard-props
function toCard(p: Paket) {
  return {
    id: p.artno,
    title: p.name,
    artno: p.artno,
    img: p.image,
    excl: p.price,
    videos: (p.videos ?? []).map(v => `${WP}/${v}`),
    bullets: p.specs ?? [],
    persons: p.persons,
    manualUrl: p.manualUrl,
    group: p.group ?? 'ljuspaket',
  };
}

const allPaket = (ljusData.paket?.products ?? []).map(p => toCard(p as Paket));
const ljuspaket = allPaket.filter(p => p.group !== 'scenpaket');
const scenpaket = allPaket.filter(p => p.group === 'scenpaket');

const faqSchema = {
  "@context": "https://schema.org",
  "@type": "FAQPage",
  "mainEntity": ljusData.paket.faq.map((f: {q:string,a:string}) => ({
    "@type": "Question",
    "name": f.q,
    "acceptedAnswer": { "@type": "Answer", "text": f.a }
  }))
};
const _low = (items: any[]) => Math.min(...items.filter((x: any) => x?.price).map((x: any) => x.price));
const _heroFranExcl = _low((ljusData as any).paket.products);
const _heroFranInkl = Math.round(_heroFranExcl * 1.25);
---
<Layout
  schemaExtra={faqSchema}
  title="Hyra Färdiga Ljuspaket Stockholm – Small till XL+ | Scenkonsult"
  description="Hyr färdiga ljuspaket i Stockholm från 399 kr/dygn. Small till XL+ – 12 paket för 10–250 personer. LED PAR, Moving Heads, Strobe, Laser. Leverans Storstockholm."
>

  <ModernHero
    eyebrow="Ljus · Färdiga paket"
    h1="Färdiga ljuspaket i"
    h1Accent="Stockholm"
    intro="Hyr färdiga ljuspaket för fest, bröllop och event i Stockholm. Elva paket från Small till XL+ plus två dedikerade scenpaket – för 10 till 250 personer. Plug & play med automatisk musiksynk, ingen DMX-kunskap krävs. Leverans i hela Storstockholm."
    breadcrumbs={[
      { label: "Hem", href: "/" },
      { label: "Hyra ljus", href: "/vara-tjanster/hyra-ljus/" },
      { label: "Färdiga ljuspaket" }
    ]}
    chips={[
      { html: `Från <span class="sk-price-excl">${_heroFranExcl.toLocaleString('sv')}&nbsp;kr</span><span class="sk-price-incl hidden">${_heroFranInkl.toLocaleString('sv')}&nbsp;kr</span> / dygn` },
      "Plug & play",
      "Automatisk musiksynk",
      "DMX med automatisk eller manuell styrning",
      "Leverans i Storstockholm",
    ]}
  />


  <section class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pb-16">
    <h2 class="text-2xl font-bold text-white mb-6 border-b border-white/10 pb-3">Färdiga ljuspaket</h2>
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
      {ljuspaket.map(p => (
        <MediaCard
          id={p.id}
          title={p.title}
          images={[p.img]}
          videos={p.videos}
          priceExcl={p.excl}
          bullets={p.bullets}
          artno={p.artno}
          tag={p.persons}
          category="Färdiga ljuspaket"
          manualUrl={p.manualUrl}
        />
      ))}
    </div>
  </section>


  <!-- SCENPAKET -->
  <section class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pb-16">
    <h2 class="text-2xl font-bold text-white mb-6 border-b border-white/10 pb-3">Scenpaket</h2>
    <p class="text-gray-400 text-sm mb-6 -mt-3">Dedikerade paket för scen, teater och föreställning — fokuserat ljus med DMX-styrning.</p>
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
      {scenpaket.map(p => (
        <MediaCard
          id={p.id}
          title={p.title}
          images={[p.img]}
          videos={p.videos}
          priceExcl={p.excl}
          bullets={p.bullets}
          artno={p.artno}
          tag={p.persons}
          category="Scenpaket"
          manualUrl={p.manualUrl}
        />
      ))}
    </div>
  </section>


  <!-- LJUSSTYRNING (DMX) -->
  <section class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pb-16">
    <h2 class="text-2xl font-bold text-white mb-6 border-b border-white/10 pb-3">Ljusstyrning (DMX)</h2>
    <div class="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-5 gap-3">
      {(ljusData.dmx?.tillbehor ?? []).map((p: any) => (
        <ElTillbehorCard img={p.image} name={p.name} excl={p.price} desc={p.description ?? p.desc ?? ''} category="DMX-styrning" artno={p.artno} modal={(p.price ?? 0) >= 400} specs={p.specs ?? []} manualUrl={p.manualUrl} />
      ))}
    </div>
  </section>

  <!-- EL-TILLBEHÖR -->
  <section class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pb-16">
    <h2 class="text-2xl font-bold text-white mb-6 border-b border-white/10 pb-3">El-tillbehör</h2>
    <div class="grid grid-cols-3 sm:grid-cols-4 lg:grid-cols-7 gap-3">
      {[
        ...(_elData.products.filter((p:any) => !p.categories || p.categories.includes('ljus'))?.filter((p:any) => ['SK-EL-0001','SK-EL-0003','SK-EL-0002'].includes(p.artno))
          .sort((a:any,b:any) => ['SK-EL-0001','SK-EL-0003','SK-EL-0002'].indexOf(a.artno) - ['SK-EL-0001','SK-EL-0003','SK-EL-0002'].indexOf(b.artno)) ?? []),
        ...(_elData.products.filter((p:any) => !p.categories || p.categories.includes('ljus'))?.filter((p:any) => !['SK-EL-0001','SK-EL-0002','SK-EL-0003'].includes(p.artno)) ?? []),
      ].map((p: any) => (
        <ElTillbehorCard img={p.image} name={p.name} excl={p.price} desc={p.description ?? p.desc ?? ''} category="El-tillbehör" artno={p.artno} priceNote={p.priceNote ?? '/dygn'} />
      ))}
    </div>
  </section>

  <!-- TEKNIKER & TJÄNSTER -->
  <section class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pb-16">
    <h2 class="text-2xl font-bold text-white mb-2 border-b border-white/10 pb-3">Tekniker & tjänster</h2>
    <p class="text-gray-400 text-sm mb-6">Kombinera ditt ljuspaket med en professionell tekniker eller scenengineer som sätter upp och kör utrustningen på plats.</p>
    <div class="grid grid-cols-2 lg:grid-cols-4 gap-4">
      {_tjansterData.services?.filter((s:any) => s.categories?.includes('ljus')).map((svc: any) => (
        <article class="bg-brand-navy border border-white/10 hover:border-brand-orange/40 rounded-2xl overflow-hidden transition-all group flex flex-col">
          <div class="bg-brand-dark overflow-hidden" style="aspect-ratio:1024/853">
            <img src={svc.image} alt={svc.alt ?? svc.name} width="800" height="665" loading="lazy"
              class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-300" />
          </div>
          <div class="p-5 flex flex-col flex-1">
            <h3 class="font-bold text-white text-lg mb-2">{svc.name}</h3>
            {svc.artno && <p class="artno-badge mb-2">{svc.artno}</p>}
            <p class="text-gray-400 text-sm mb-3 flex-1">{svc.description}</p>
            <ul class="space-y-1 mb-4 text-xs text-gray-400 border-t border-white/10 pt-3">
              {svc.includes?.map((item: string) => (
                <li class="flex gap-1.5"><span class="text-green-400">▸</span>{item}</li>
              ))}
            </ul>
            <div class="mt-auto flex items-center justify-between border-t border-white/10 pt-3 gap-3">
              <div class="leading-none">
                <span class="sk-price-excl text-brand-orange font-extrabold text-xl">{svc.price.toLocaleString('sv')} kr</span>
                <span class="sk-price-incl hidden text-brand-orange font-extrabold text-xl">{Math.round(svc.price * 1.25).toLocaleString('sv')} kr</span>
                <span class="text-gray-400 text-xs">{svc.priceNote ?? '/tim'}</span>
              </div>
              <CartButton id={svc.artno} name={svc.name} price={svc.price} category="Tjänster" artno={svc.artno} image={svc.image} size="md" iconOnly={true} />
            </div>
          </div>
        </article>
      ))}
    </div>
  </section>
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pb-16 flex flex-wrap gap-4">
    <a href="/vara-tjanster/hyra-ljus/" class="border border-white/20 text-gray-300 hover:border-brand-orange hover:text-brand-orange px-5 py-3 rounded-xl text-sm font-semibold transition-all">← Tillbaka till ljus</a>
    <a href="/vara-tjanster/hyra-ljus/ljuseffekter/" class="border border-white/30 text-white hover:bg-white/10 px-5 py-3 rounded-xl text-sm font-semibold transition-colors">Lösa effekter →</a>
    <a href="/bokningssida/" class="bg-brand-orange hover:bg-brand-orange-light text-white px-5 py-3 rounded-xl text-sm font-semibold transition-colors">Boka nu</a>
  </div>

  <KunskapBlock
    guides={[
    { href: "/for/guider/dmx-styrning-guide/", label: "DMX-styrning — komplett guide" },
    { href: "/for/guider/ljussattning-tips/", label: "Ljussättningstips för event" },
    { href: "/for/guider/hyra-uplights/", label: "Hyra uplights — guide" },
    { href: "/for/guider/ljus-brollop/", label: "Ljus till bröllop" }
  ]}
    videos={[
    { slug: "led-teknik", label: "LED-teknik — egenskaper & funktioner" },
    { slug: "hur-det-fungerar", label: "Hur det fungerar att hyra hos oss" }
  ]}
  />

</Layout>
